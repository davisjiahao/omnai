# Legacy alias: `security-change`

This page is retained only for compatibility with older OmnAI identifiers.

Canonical profile: [`architecture-governance`](architecture-governance.md)

The CLI resolves `security-change` to `architecture-governance`. Security impact still raises the security risk dimension, review lens, and evidence requirements; this alias is not a separate canonical profile.
