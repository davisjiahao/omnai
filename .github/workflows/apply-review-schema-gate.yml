name: Apply structured review gate

on:
  push:
    branches:
      - feat/omnai-v0.1
    paths:
      - .omnai-maintenance/review-schema-gate

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/omnai-v0.1
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: python scripts/apply-review-schema-gate.py
      - run: |
          npm install
          npm run typecheck
          npm test
          npm run build
          npm pack --dry-run
          git diff --check
      - name: Commit verified review gate
        run: |
          rm -f scripts/apply-review-schema-gate.py
          rm -f .omnai-maintenance/review-schema-gate
          rm -f .github/workflows/apply-review-schema-gate.yml
          rmdir .omnai-maintenance 2>/dev/null || true
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix: require structured fresh review evidence"
          git push origin HEAD:feat/omnai-v0.1
