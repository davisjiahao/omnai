# Legacy alias: `frontend-feature`

This page is retained only for compatibility with older OmnAI identifiers.

Canonical profile: [`ui-ux-feature`](ui-ux-feature.md)

The CLI resolves `frontend-feature` to `ui-ux-feature`. Do not treat this alias as a separate scenario or maintain separate routing rules here.
