# OmnAI v0.3 权限修复 Preflight 实施计划

> **供 Agent Worker 执行：** 必须使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans，逐任务执行本计划。所有步骤使用复选框跟踪。

**目标（Goal）：** 在权限核心开发前，移除不可信的 .git 文件系统回退，并让默认测试入口稳定发现所有模块 test 目录中的编译测试。

**架构（Architecture）：** 仓库根识别从 paths.ts 拆到单一职责的 repository-root.ts，只信任成功的 git rev-parse，并通过内部命令网关注入测试 Git 启动失败。测试发现逻辑拆到无副作用的 tooling 模块，使用确定性、顺序递归，run-tests.mjs 只负责调用和启动 Node test runner。

**技术栈（Tech Stack）：** Node.js 20+、TypeScript 5.9 strict、Node test runner、Git CLI、ES Module。

**规格（Spec）：** docs/superpowers/specs/2026-08-22-omnai-v0.3-change-authority-integrity-repair-design.md 第 9.1、12.1、13、14 节。

## 全局约束

- 只在 /workspace/scratch/a43a4e5fdb99/omnai-codebase-design-fusion 的 feat/omnai-v0.3-full-pipeline-fusion 分支执行。
- package.json 和 src/version.ts 保持 0.2.0；本计划不得生成 WorkflowLock 或启用 micro-change。
- 不新增 npm 依赖，不修改 src/execution。
- 相对远程主干新增或修改的测试放在对应模块的 test 目录。
- 新增或修改代码使用中文注释说明背景、目的和必要上下文；提交信息使用中文。
- 本计划只允许修改下文列出的文件。工作区内其他未跟踪计划和用户改动不得修改、删除、暂存或提交。
- 每个行为先观察 RED，再实现最小代码观察 GREEN。
- 代码评审、编译、测试、打包和日志核验由实际后台子 Agent 执行，并返回命令、环境、退出码和证据。
- npm 命令使用独立临时缓存，避免继承无权限的全局缓存配置。
- 未经用户明确要求不得 push。

---

## 文件结构

| 文件 | 责任 |
| --- | --- |
| src/core/repository-root.ts | Git 仓库根解析、稳定错误和内部可注入 Git 命令边界 |
| src/core/paths.ts | 保持现有路径 API，并重新导出 findRepositoryRoot |
| src/core/test/repository-root.test.ts | 仓库根正向、负向、cause 和无弱回退测试 |
| src/core/test/paths.test.ts | 删除当前过渡测试，由 repository-root.test.ts 取代 |
| src/tooling/compiled-test-discovery.ts | 确定性、顺序递归发现编译测试 |
| src/tooling/test/compiled-test-discovery.test.ts | 嵌套测试发现、过滤和排序测试 |
| scripts/run-tests.mjs | 调用编译后的发现模块并启动 Node test runner |

### Task 1：只信任 Git 的仓库根识别

**Files：**

- Create: src/core/repository-root.ts
- Modify: src/core/paths.ts
- Delete: src/core/test/paths.test.ts
- Create: src/core/test/repository-root.test.ts

**Interfaces：**

- Produces: RepositoryRootErrorCode = 'GIT_UNAVAILABLE' | 'REPOSITORY_NOT_FOUND'
- Produces: RepositoryRootError extends Error，公开只读 code
- Produces: GitRepositoryRootResolver = (startDirectory: string) => string
- Produces: resolveRepositoryRoot(startDirectory, resolveWithGit): string，仅供 Core 内部和模块测试
- Preserves: findRepositoryRoot(startDirectory = process.cwd()): string

- [ ] **Step 1：用新的模块测试替换弱回退测试**

删除 src/core/test/paths.test.ts，新增 src/core/test/repository-root.test.ts：

~~~ts
import assert from 'node:assert/strict';
import { execFileSync } from 'node:child_process';
import { mkdir, mkdtemp, rm } from 'node:fs/promises';
import { tmpdir } from 'node:os';
import { join, resolve } from 'node:path';
import { afterEach, test } from 'node:test';
import {
  findRepositoryRoot,
  RepositoryRootError,
  type RepositoryRootErrorCode,
  resolveRepositoryRoot,
} from '../repository-root.js';

// 背景：权限核心必须从真实 Git 解析仓库身份，祖先目录中的占位 .git 不能成为授权依据。
// 目的：固定 Git 成功、非仓库、命令缺失、权限失败和目录竞态的稳定错误语义。
// 上下文：Git 失败通过模块内命令网关注入，不修改整个测试进程的 PATH，避免并发测试互相污染。
const cleanupDirectories: string[] = [];

afterEach(async () => {
  while (cleanupDirectories.length > 0) {
    const directory = cleanupDirectories.pop();
    if (directory) await rm(directory, { recursive: true, force: true });
  }
});

async function temporaryDirectory(prefix: string): Promise<string> {
  const directory = await mkdtemp(join(tmpdir(), prefix));
  cleanupDirectories.push(directory);
  return directory;
}

function failureWithCode(code: string): Error {
  return Object.assign(new Error('模拟 Git 启动失败'), { code });
}

function expectRepositoryRootError(
  action: () => unknown,
  expectedCode: RepositoryRootErrorCode,
  expectedCause?: unknown,
): RepositoryRootError {
  let caught: unknown;
  try {
    action();
  } catch (error) {
    caught = error;
  }
  assert.ok(caught instanceof RepositoryRootError);
  assert.equal(caught.code, expectedCode);
  if (expectedCause !== undefined) assert.equal(caught.cause, expectedCause);
  return caught;
}

test('成功的 Git 结果是唯一仓库根来源', async () => {
  const root = await temporaryDirectory('omnai-repository-root-');
  const nested = join(root, 'nested', 'work');
  await mkdir(nested, { recursive: true });

  const actual = resolveRepositoryRoot(nested, (cwd) => {
    assert.equal(cwd, resolve(nested));
    return root + '\n';
  });

  assert.equal(actual, resolve(root));
});

test('Git 判定非仓库时不接受祖先占位 .git', async () => {
  const root = await temporaryDirectory('omnai-placeholder-git-');
  const nested = join(root, 'nested', 'work');
  await mkdir(join(root, '.git'), { recursive: true });
  await mkdir(nested, { recursive: true });
  const cause = Object.assign(new Error('not a git repository'), { status: 128 });

  expectRepositoryRootError(
    () => resolveRepositoryRoot(nested, () => {
      throw cause;
    }),
    'REPOSITORY_NOT_FOUND',
    cause,
  );
});

test('Git 命令不存在时返回 GIT_UNAVAILABLE 并保留 cause', async () => {
  const root = await temporaryDirectory('omnai-git-missing-');
  const cause = failureWithCode('ENOENT');

  expectRepositoryRootError(
    () => resolveRepositoryRoot(root, () => {
      throw cause;
    }),
    'GIT_UNAVAILABLE',
    cause,
  );
});

test('Git 命令无执行权限时返回 GIT_UNAVAILABLE 并保留 cause', async () => {
  const root = await temporaryDirectory('omnai-git-eacces-');
  const cause = failureWithCode('EACCES');

  expectRepositoryRootError(
    () => resolveRepositoryRoot(root, () => {
      throw cause;
    }),
    'GIT_UNAVAILABLE',
    cause,
  );
});

test('起始目录不存在时不调用 Git 并保留文件系统 cause', async () => {
  const root = await temporaryDirectory('omnai-missing-start-');
  const missing = join(root, 'deleted', 'work');
  let called = false;

  const error = expectRepositoryRootError(
    () => resolveRepositoryRoot(missing, () => {
      called = true;
      return root;
    }),
    'REPOSITORY_NOT_FOUND',
  );

  assert.equal(called, false);
  assert.equal((error.cause as NodeJS.ErrnoException).code, 'ENOENT');
});

test('默认入口能够解析真实 Git 仓库', async () => {
  const root = await temporaryDirectory('omnai-real-git-');
  const nested = join(root, 'nested');
  execFileSync('git', ['init', '--quiet'], { cwd: root, stdio: 'ignore' });
  await mkdir(nested);
  const expected = execFileSync('git', ['rev-parse', '--show-toplevel'], {
    cwd: nested,
    encoding: 'utf8',
  }).trim();

  assert.equal(findRepositoryRoot(nested), expected);
});
~~~

- [ ] **Step 2：执行 RED 验证**

由后台编译 Agent 执行：

~~~bash
env -u NPM_CONFIG_CACHE npm_config_cache=/tmp/omnai-authority-preflight-npm-cache npm run build
~~~

Expected：FAIL，TypeScript 报告找不到 ../repository-root.js；不能因旧 paths.test.ts 仍存在而误判通过。

- [ ] **Step 3：实现 Git-only 仓库根模块**

新增 src/core/repository-root.ts：

~~~ts
import { execFileSync } from 'node:child_process';
import { statSync } from 'node:fs';
import { resolve } from 'node:path';

export type RepositoryRootErrorCode =
  | 'GIT_UNAVAILABLE'
  | 'REPOSITORY_NOT_FOUND';

export class RepositoryRootError extends Error {
  readonly code: RepositoryRootErrorCode;

  constructor(code: RepositoryRootErrorCode, startDirectory: string, cause?: unknown) {
    const detail = code === 'GIT_UNAVAILABLE'
      ? 'Git executable is unavailable'
      : 'No Git repository found';
    super(
      code + ': ' + detail + ' from ' + startDirectory,
      cause === undefined ? undefined : { cause },
    );
    this.name = 'RepositoryRootError';
    this.code = code;
  }
}

export type GitRepositoryRootResolver = (startDirectory: string) => string;

const GIT_LAUNCH_FAILURE_CODES = new Set([
  'ENOENT',
  'EACCES',
  'ENOEXEC',
  'ETXTBSY',
]);

export function findRepositoryRoot(startDirectory = process.cwd()): string {
  return resolveRepositoryRoot(startDirectory, resolveWithSystemGit);
}

export function resolveRepositoryRoot(
  startDirectory: string,
  resolveWithGit: GitRepositoryRootResolver,
): string {
  const resolvedStartDirectory = requireExistingDirectory(startDirectory, startDirectory);
  let candidate: string;

  try {
    candidate = resolveWithGit(resolvedStartDirectory).trim();
  } catch (cause) {
    // 背景：execFileSync 的 ENOENT 既可能表示 Git 不存在，也可能表示 cwd 在校验后被删除。
    // 目的：再次确认 cwd，避免把目录竞态误报成 Git 安装问题，同时完整保留底层 cause。
    // 上下文：这里不扫描 .git；任何 Git 启动失败都只能失败关闭。
    if (isGitLaunchFailure(cause) && isExistingDirectory(resolvedStartDirectory)) {
      throw new RepositoryRootError('GIT_UNAVAILABLE', startDirectory, cause);
    }
    throw new RepositoryRootError('REPOSITORY_NOT_FOUND', startDirectory, cause);
  }

  if (candidate.length === 0) {
    throw new RepositoryRootError(
      'REPOSITORY_NOT_FOUND',
      startDirectory,
      new Error('git rev-parse returned an empty repository root'),
    );
  }

  return requireExistingDirectory(candidate, startDirectory);
}

function resolveWithSystemGit(startDirectory: string): string {
  return execFileSync('git', ['rev-parse', '--show-toplevel'], {
    cwd: startDirectory,
    encoding: 'utf8',
    stdio: ['ignore', 'pipe', 'ignore'],
  });
}

function requireExistingDirectory(directory: string, requestedStart: string): string {
  const resolvedDirectory = resolve(directory);
  try {
    if (statSync(resolvedDirectory).isDirectory()) return resolvedDirectory;
  } catch (cause) {
    throw new RepositoryRootError('REPOSITORY_NOT_FOUND', requestedStart, cause);
  }
  throw new RepositoryRootError('REPOSITORY_NOT_FOUND', requestedStart);
}

function isExistingDirectory(directory: string): boolean {
  try {
    return statSync(directory).isDirectory();
  } catch {
    return false;
  }
}

function isGitLaunchFailure(failure: unknown): boolean {
  const code = (failure as NodeJS.ErrnoException).code;
  return typeof code === 'string' && GIT_LAUNCH_FAILURE_CODES.has(code);
}
~~~

将 src/core/paths.ts 顶部改为：

~~~ts
import { join } from 'node:path';

export { findRepositoryRoot } from './repository-root.js';
~~~

paths.ts 其余路径函数保持原样，不做重构。

- [ ] **Step 4：执行 GREEN 与调用方回归**

由后台测试 Agent 执行：

~~~bash
env -u NPM_CONFIG_CACHE npm_config_cache=/tmp/omnai-authority-preflight-npm-cache npm run build
node --test dist/src/core/test/repository-root.test.js
node --test dist/test/host-context-cli.test.js
~~~

Expected：三个命令退出码均为 0；仓库根模块全部用例通过，Host Context CLI 现有用例无回归。

- [ ] **Step 5：检查并提交 Task 1**

~~~bash
git diff --check -- src/core/repository-root.ts src/core/paths.ts src/core/test/repository-root.test.ts
git add src/core/repository-root.ts src/core/paths.ts src/core/test/repository-root.test.ts
git commit -m "修复：仅信任 Git 解析仓库根"
~~~

提交前确认 src/core/test/paths.test.ts 已从工作区删除且没有进入提交；其他计划文件不得暂存。

### Task 2：确定性递归测试发现

**Files：**

- Create: src/tooling/compiled-test-discovery.ts
- Create: src/tooling/test/compiled-test-discovery.test.ts
- Modify: scripts/run-tests.mjs

**Interfaces：**

- Produces: findCompiledTestFiles(rootDirectory: string): Promise<string[]>
- Consumes: scripts/run-tests.mjs 在 npm run build 完成后导入 dist/src/tooling/compiled-test-discovery.js
- Preserves: npm test 仍以 Node --test 一次运行全部编译测试

- [ ] **Step 1：编写嵌套发现 RED 测试**

新增 src/tooling/test/compiled-test-discovery.test.ts：

~~~ts
import assert from 'node:assert/strict';
import { mkdir, mkdtemp, rm, writeFile } from 'node:fs/promises';
import { tmpdir } from 'node:os';
import { join, resolve } from 'node:path';
import { afterEach, test } from 'node:test';
import { findCompiledTestFiles } from '../compiled-test-discovery.js';

// 背景：模块测试编译后分布在 dist/src/**/test，固定扫描 dist/test 会静默漏掉安全回归。
// 目的：固定递归、过滤和跨文件系统稳定排序，测试入口不依赖 readdir 的返回顺序。
// 上下文：发现过程顺序遍历目录，避免大型仓库中无界 Promise.all 同时打开过多目录。
const cleanupDirectories: string[] = [];

afterEach(async () => {
  while (cleanupDirectories.length > 0) {
    const directory = cleanupDirectories.pop();
    if (directory) await rm(directory, { recursive: true, force: true });
  }
});

test('递归发现全部 test.js 并返回确定性绝对路径', async () => {
  const root = await mkdtemp(join(tmpdir(), 'omnai-test-discovery-'));
  cleanupDirectories.push(root);
  const legacy = join(root, 'test', 'legacy.test.js');
  const core = join(root, 'src', 'core', 'test', 'core.test.js');
  const ignored = join(root, 'src', 'core', 'test', 'helper.js');
  await mkdir(join(root, 'test'), { recursive: true });
  await mkdir(join(root, 'src', 'core', 'test'), { recursive: true });
  await writeFile(legacy, '');
  await writeFile(core, '');
  await writeFile(ignored, '');

  assert.deepEqual(
    await findCompiledTestFiles(root),
    [resolve(core), resolve(legacy)].sort(compareText),
  );
});

test('没有编译测试时返回空集合', async () => {
  const root = await mkdtemp(join(tmpdir(), 'omnai-empty-discovery-'));
  cleanupDirectories.push(root);

  assert.deepEqual(await findCompiledTestFiles(root), []);
});

function compareText(left: string, right: string): number {
  if (left < right) return -1;
  if (left > right) return 1;
  return 0;
}
~~~

- [ ] **Step 2：执行 RED 验证**

由后台编译 Agent 执行：

~~~bash
env -u NPM_CONFIG_CACHE npm_config_cache=/tmp/omnai-authority-preflight-npm-cache npm run build
~~~

Expected：FAIL，TypeScript 报告找不到 compiled-test-discovery.js。

- [ ] **Step 3：实现顺序递归发现并接入 runner**

新增 src/tooling/compiled-test-discovery.ts：

~~~ts
import { readdir } from 'node:fs/promises';
import { join, resolve } from 'node:path';

export async function findCompiledTestFiles(rootDirectory: string): Promise<string[]> {
  const files: string[] = [];
  await collectCompiledTests(resolve(rootDirectory), files);
  return files.sort(compareText);
}

async function collectCompiledTests(
  currentDirectory: string,
  files: string[],
): Promise<void> {
  // 背景：编译测试已经迁移到多个模块目录，递归发现是默认测试入口完整性的组成部分。
  // 目的：顺序读取排序后的目录项，既保证清单确定，也避免无界 Promise.all 耗尽文件描述符。
  // 上下文：这里只收集常规 .test.js 文件；符号链接和其他目录项不会被递归跟随。
  const entries = await readdir(currentDirectory, { withFileTypes: true });
  entries.sort((left, right) => compareText(left.name, right.name));

  for (const entry of entries) {
    const path = join(currentDirectory, entry.name);
    if (entry.isDirectory()) {
      await collectCompiledTests(path, files);
    } else if (entry.isFile() && entry.name.endsWith('.test.js')) {
      files.push(resolve(path));
    }
  }
}

function compareText(left: string, right: string): number {
  if (left < right) return -1;
  if (left > right) return 1;
  return 0;
}
~~~

将 scripts/run-tests.mjs 完整调整为：

~~~js
import { spawnSync } from 'node:child_process';
import { resolve } from 'node:path';
import { findCompiledTestFiles } from '../dist/src/tooling/compiled-test-discovery.js';

const directory = resolve('dist');
const files = await findCompiledTestFiles(directory);

if (files.length === 0) {
  console.error('No compiled test files found under ' + directory);
  process.exit(1);
}

const result = spawnSync(process.execPath, ['--test', ...files], {
  cwd: process.cwd(),
  stdio: 'inherit',
});

process.exit(result.status ?? 1);
~~~

- [ ] **Step 4：执行 GREEN 与默认入口验证**

由后台测试 Agent 执行：

~~~bash
env -u NPM_CONFIG_CACHE npm_config_cache=/tmp/omnai-authority-preflight-npm-cache npm run build
node --test dist/src/tooling/test/compiled-test-discovery.test.js
node scripts/run-tests.mjs
~~~

Expected：全部退出码为 0；默认入口包含 dist/src/core/test/repository-root.test.js 和 dist/src/tooling/test/compiled-test-discovery.test.js，测试总数不得低于规格记录的 783 项基线。

- [ ] **Step 5：检查并提交 Task 2**

~~~bash
git diff --check -- scripts/run-tests.mjs src/tooling/compiled-test-discovery.ts src/tooling/test/compiled-test-discovery.test.ts
git add scripts/run-tests.mjs src/tooling/compiled-test-discovery.ts src/tooling/test/compiled-test-discovery.test.ts
git commit -m "测试：递归发现模块编译测试"
~~~

### Task 3：独立复审和完整基线认证

**Files：**

- Review only: Task 1 与 Task 2 的提交
- Evidence: /tmp/omnai-authority-preflight-review.md
- Evidence: /tmp/omnai-authority-preflight-build.md
- Evidence: /tmp/omnai-authority-preflight-tests.md
- Evidence: /tmp/omnai-authority-preflight-adversarial.md
- Evidence: /tmp/omnai-authority-preflight-package.md

**Interfaces：**

- Consumes: 前两项提交和权限核心规格
- Produces: 可进入 AR-A 的绿色基线及独立评审结论

- [ ] **Step 1：后台代码复审**

启动独立代码评审 Agent，对前两项提交逐条核对：

- 不存在 .git 祖先扫描或第二次 Git 探测。
- Git ENOENT、EACCES、非仓库和目录缺失均保留 cause。
- 测试没有修改全局 process.env.PATH。
- 测试发现使用稳定排序和顺序或有界递归。
- 新测试位于模块 test 目录。
- 没有升版、WorkflowLock、micro-change 或 src/execution 改动。

评审结果写入 /tmp/omnai-authority-preflight-review.md。存在 Critical 或 Important 时停止本任务，回到对应 Task 先补 RED 测试、修复、复跑并提交；不得以接受风险方式继续。

- [ ] **Step 2：后台编译验证**

~~~bash
env -u NPM_CONFIG_CACHE npm_config_cache=/tmp/omnai-authority-preflight-npm-cache npm run typecheck
env -u NPM_CONFIG_CACHE npm_config_cache=/tmp/omnai-authority-preflight-npm-cache npm run build
~~~

Expected：两个命令退出码均为 0。记录 Node、npm、TypeScript 版本和命令输出到 /tmp/omnai-authority-preflight-build.md。

- [ ] **Step 3：并行执行完整测试和对抗测试**

完整测试 Agent：

~~~bash
env -u NPM_CONFIG_CACHE npm_config_cache=/tmp/omnai-authority-preflight-npm-cache npm test
~~~

对抗测试 Agent：

~~~bash
env -u NPM_CONFIG_CACHE npm_config_cache=/tmp/omnai-authority-preflight-npm-cache npm run test:adversarial
~~~

Expected：全部退出码为 0；完整测试不少于 783 项，对抗测试不少于 173 项，并记录实际通过、失败、跳过数量。

- [ ] **Step 4：后台打包与安装验证**

~~~bash
preflight_pack_dir="$(mktemp -d /tmp/omnai-preflight-pack.XXXXXX)"
preflight_install_dir="$(mktemp -d /tmp/omnai-preflight-install.XXXXXX)"
env -u NPM_CONFIG_CACHE npm_config_cache=/tmp/omnai-authority-preflight-npm-cache npm pack --pack-destination "$preflight_pack_dir"
cd "$preflight_install_dir"
env -u NPM_CONFIG_CACHE npm_config_cache=/tmp/omnai-authority-preflight-npm-cache npm init -y
env -u NPM_CONFIG_CACHE npm_config_cache=/tmp/omnai-authority-preflight-npm-cache npm install --ignore-scripts "$preflight_pack_dir/omnai-0.2.0.tgz"
./node_modules/.bin/omnai --version
~~~

Expected：安装成功，输出 0.2.0。记录 tarball 路径、SHA-256、安装命令和输出到 /tmp/omnai-authority-preflight-package.md。

- [ ] **Step 5：最终工作区核验**

~~~bash
git diff --check
git status --short --branch
git log -3 --oneline --decorate
~~~

Expected：

- Preflight 代码由两个中文提交组成。
- 工作区只保留执行前已经存在、且本计划未拥有的改动或未跟踪文件。
- 没有未提交的 Preflight 源码。
- 没有 push。

主 Agent 汇总五份证据。只有评审无 Critical/Important、所有验证为绿色时，才把 Preflight 标记完成并开始编写 AR-A 计划。
