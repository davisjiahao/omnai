# Omnai v0.3 原生全流程收敛 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking. 当前环境按已批准的串行方式执行；每个代码行为遵守 TDD，完成声明前执行 `verification-before-completion`。

**Goal:** 从 `948d927` 恢复一个可构建、可测试、可打包、可认证的 final-v0.3 原生全流程，同时吸收旧恢复权限分支的有效不变量并避免旧模型回流。

**Architecture:** 以 final-v0.3 Schema、`ChangeAuthorityContext`、`AuthorityEnvelope`、持久 owner 和 `MachineAuthoritySeal` 为唯一权限架构。先用 transition ownership gate 管理 Plan01 形成的跨计划迁移债务，再按 Plans02–09 逐步关闭 Context、Project、Core、Stage、Workspace、Host/CLI、Audit 和发布边界；旧 RecoveryAuthority 只提供不变量，不形成第二个运行时。

**Tech Stack:** Node.js `>=20`、TypeScript `^5.9.2` strict、Zod `^4.1.5`、YAML `^2.8.1`、Commander `^14.0.0`、Node test runner、原生 Git 对象/索引与 Linux verified-fd provider。

**Spec:** `docs/superpowers/specs/2026-08-27-omnai-v0.3-native-pipeline-convergence-design.md`

## 全局约束

- 只在 `feat/omnai-v0.3-native-pipeline-convergence` 和对应隔离 worktree 中写入。
- 基线固定为 `948d927601007720d6051b4c7a2cf6d0fbbb96ad`；旧工作区保持原状。
- final-v0.3 不兼容 v0.2 或任何中间 v0.3。
- 不修改 `src/execution/**`；Plan07 只能将其原样退出 native graph。
- 每项代码先 RED 再 GREEN；新增测试进入对应 `src/<module>/test/` 目录。
- 代码注释和维护文档使用中文；Git commit message 使用中文。
- 不新增依赖，除非先重新裁决设计；不 push、merge、publish 或 release，除非用户明确要求。
- 原十份 final-v0.3 计划仍提供任务级文件清单和测试矩阵；本计划只覆盖分支、基线、顺序、RecoveryAuthority 映射与诊断过渡条款。

## Stage 0：收敛基线与诊断门

### Task 0.1：固定需求与计划依据

- [x] 提交 master + Plans01–09、诊断治理计划、RecoveryAuthority 历史规格/计划。
- [x] 提交 2026-08-27 收敛设计与本计划。
- [x] 明确历史 RecoveryAuthority 文件只提供不变量，不提供 native 可执行文件清单。

### Task 0.2：建立可执行 transition ownership gate

按 `2026-08-25-omnai-v0.3-diagnostic-ownership-governance.md` Task 1 TDD 实施：

- [x] 写 diagnostic parser/evaluator RED 测试。
- [x] 建立 exact ownership manifest，并显式裁决 17 个跨计划 `Files` 重叠。
- [x] 实现 `npm run check:transition -- --completed-through=P01`。
- [x] 对 `948d927` fresh 实跑并得到 `888/76`、`pastOwner=0`、`unowned=0`。
- [x] 单列 Plan07 的八个 Execution 文件及其诊断，禁止修改消债。
- [x] 证明无路径 TypeScript diagnostic、到期未 `CLOSED` 条目及携带诊断的 `CLOSED` 条目全部失败关闭。
- [x] 证明非 canonical/traversal path 与外指 plan symlink 全部失败关闭。
- [x] 提交 Stage 0；提交中不得包含业务生产代码或旧工作区文件。

## Stage 1：Plan02 Authority Context 与 Source Resolution

执行 `2026-08-23-omnai-v0.3-02-authority-context-source-resolution.md`。入口要求：Plan01 聚焦验证保持全绿，transition gate 在 P01 无 past-owner/unowned。退出时运行 `completed-through=P02`，P02 owner diagnostics 必须为零。

## Stage 2：Plan03 Project Genesis 与全局链

执行 `2026-08-23-omnai-v0.3-03-project-genesis-global-chain.md`。所有 Change Genesis 和后续 top-level owner 必须进入唯一 `AuthorityEnvelope` chain；任何恢复状态由 frozen owner payload 和 exact prefix 认证。

## Stage 3：Plan04 Core Writer 与 native RecoveryAuthority 收口

执行 `2026-08-23-omnai-v0.3-04-core-writer-closure.md`，并把 RecoveryAuthority 不变量作为以下附加验收：

- [ ] ordinary、Decision、Flow 每个 fresh/PENDING/partial/completed/response-loss 路径先构建同一认证 context。
- [ ] owner candidate 由 chain/inventory 确定，不以待验证 correlation、DecisionId、from/to 缩小集合。
- [ ] 当前机器投影只允许 frozen target publication 的唯一精确前缀。
- [ ] commit 不重新读取业务状态、不重新编译目标、不调用 caller callback。
- [ ] generic durable writer 和 `UNOWNED_INTERNAL` 写入逃生口物理不存在。
- [ ] completed validator 与 PENDING recovery 使用同一 owner/chain/payload 规则。

## Stage 4：Plan05 Stage 与 Evidence 生命周期

执行 `2026-08-23-omnai-v0.3-05-stage-evidence-lifecycle.md`。每个 Run/receipt 都是全局链 owner；negative Evidence、verification ticket、Task-set CAS 与 repository retry lineage 必须可精确恢复。

## Stage 5：Plan06 Workspace 与 Workset Publication

执行 `2026-08-23-omnai-v0.3-06-workspace-workset-publication.md`。WOP、Home coordinator、Project reservation、raw Git worktree publication 各自遵守 frozen target、exact prefix 和 provider binding 规则。

## Stage 6：Plan07 Host、CLI、Package 与全仓绿色

执行 `2026-08-23-omnai-v0.3-07-host-cli-package-surface.md` 和诊断治理计划 Task 3。必须：

- [ ] native modules 对 `src/execution/**` 的 import 为零。
- [ ] `src/execution/**` 相对 `948d927` 无 diff。
- [ ] native tsconfig、dist、package 和 root exports 不含旧 Execution。
- [ ] `npm run typecheck`、`npm run build`、`npm test` 恢复 exit 0。
- [ ] transition gate `completed-through=P07` 无 past-owner、unowned 或 execution blocker。

## Stage 7：Plan08 Audit 与 Scenario 终态闭环

执行 `2026-08-23-omnai-v0.3-08-audit-scenario-closure.md`。RecoveryAuthority 的 audit/terminal 不变量在此最终关闭：所有 owner 复用 exact ordered outbox；ordinary 唯一终态由一个纯编译器生成；Scenario 只绑定相同 ordinary target 的 nested digest。

## Stage 8：Plan09 发布再认证

执行 `2026-08-23-omnai-v0.3-09-publication-recertification.md` 和诊断治理计划 Task 4。发布前必须把 `check:transition` 的 tooling 编译移到隔离临时目录，或证明随后执行的 clean build 会清除其当前写入 `dist` 的非标准 declaration/source-map 产物；不能让 package bytes 依赖命令顺序。只有全量、对抗、clean checkout、packed install、compiled CLI E2E、crash recovery、双门 candidate 与最终评审全部通过，才能把分支标记为可交付。

## Stage 9：`micro-change` 轻量通道

Plan09 完成后重新审阅 `2026-08-22-omnai-v0.3-micro-change-design.md` 对最终 catalog/transaction ABI 的适配，再单独形成 TDD 实施计划。不得在前八个阶段夹带其场景、CLI、目录或 writer 改动。

## 当前检查点完成定义

Stage 0 只有在以下条件同时成立时才完成：

1. 新分支/worktree 基于精确 `948d927`，旧工作区无写入。
2. 计划与规格依据均已纳入版本控制。
3. baseline 真实复现为 `888 diagnostics / 76 files`。
4. transition ownership focused tests通过。
5. `completed-through=P01` 对真实输出给出 `pastOwner=0`、`unowned=0`，并如实保留 future debt。
6. `git diff --check` 通过，提交范围只包含文档、诊断 tooling、manifest 与 package script。
